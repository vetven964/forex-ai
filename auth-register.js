// V-TRADE AI registration patch helper.
// server-launcher.js injects the route into the existing Express app so the
// registration flow uses the same server/storage/auth environment.
module.exports = { version: '1.0.0' };
