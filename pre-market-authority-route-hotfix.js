/* V-TRADE AI — Pre-Market authoritative MT5 route V4
 * Single authoritative processing source.
 * Uses CLOSED MT5 candles for structure and live broker quote for price.
 * NEVER authorizes an order and NEVER sends Telegram.
 */
'use strict';
const fs=require('fs');
const path=require('path');
const SERVER=path.join(__dirname,'server.js');
const MARKER='VTRADE_PREMARKET_AUTHORITY_ROUTE_V4';

function inject(source){
  if(!source) return source;
  if(source.includes(MARKER)) return source;
  const anchor='const app = express();';
  if(!source.includes(anchor)) throw new Error('server app marker not found');
  const code=String.raw`
/* ${MARKER} */
(function installPreMarketAuthorityV4(app){
  if(!app||app.__VTRADE_PREMARKET_AUTHORITY_V4__)return;
  app.__VTRADE_PREMARKET_AUTHORITY_V4__=true;
  const TFS=['M5','M15','H1','H4','D1'],CORE=['M5','M15','H1','H4'];
  const W={M5:1,M15:2,H1:3,H4:4,D1:5},MIN=30;
  const n=v=>Number.isFinite(Number(v))?Number(v):null,clamp=v=>Math.max(0,Math.min(100,Number(v)||0));
  const arr=x=>Array.isArray(x)?x:Array.isArray(x?.candles)?x.candles:Array.isArray(x?.bars)?x.bars:Array.isArray(x?.history)?x.history:[];
  const norm=x=>({t:n(x?.t??x?.time??x?.timestamp??x?.timeMs),o:n(x?.o??x?.open),h:n(x?.h??x?.high),l:n(x?.l??x?.low),c:n(x?.c??x?.close),v:n(x?.v??x?.volume??x?.tickVolume)??0});
  const clean=a=>arr(a).map(norm).filter(x=>[x.o,x.h,x.l,x.c].every(Number.isFinite)).sort((a,b)=>(a.t??0)-(b.t??0));
  const avg=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:null;
  function atr(c,p=14){if(c.length<p+1)return null;const tr=[];for(let i=1;i<c.length;i++)tr.push(Math.max(c[i].h-c[i].l,Math.abs(c[i].h-c[i-1].c),Math.abs(c[i].l-c[i-1].c)));return avg(tr.slice(-p));}
  function feed(tf){let f=[];try{f=clean(typeof brokerFeed!=='undefined'?brokerFeed?.timeframes?.[tf]:null);}catch(_){}if(f.length>=MIN)return{bars:f,source:'brokerFeed.timeframes'};try{if(typeof parseBrokerCandles==='function'){const p=clean(parseBrokerCandles(tf));if(p.length>=MIN)return{bars:p,source:'parseBrokerCandles'};}}catch(e){console.warn('[PRE-MARKET V4] parser',tf,e?.message||e);}return{bars:f,source:f.length?'brokerFeed.timeframes':'none'};}
  function live(){try{if(typeof brokerLivePrice==='function'){const q=brokerLivePrice();if(q&&n(q.price)!=null)return q;}}catch(_){}return null;}
  function candle(c){if(c.length<2)return{ready:false};const x=c[c.length-1],p=c[c.length-2],r=Math.max(x.h-x.l,1e-9),b=Math.abs(x.c-x.o),up=x.h-Math.max(x.o,x.c),lo=Math.min(x.o,x.c)-x.l,pos=(x.c-x.l)/r;return{ready:true,open:x.o,high:x.h,low:x.l,close:x.c,bodyPct:+(b/r*100).toFixed(1),upperWickPct:+(up/r*100).toFixed(1),lowerWickPct:+(lo/r*100).toFixed(1),closePosition:+(pos*100).toFixed(1),hammer:lo>=b*2&&up<=Math.max(b*.8,r*.15)&&pos>.55,shootingStar:up>=b*2&&lo<=Math.max(b*.8,r*.15)&&pos<.45,bullishEngulfing:x.c>x.o&&p.c<p.o&&x.o<=p.c&&x.c>=p.o,bearishEngulfing:x.c<x.o&&p.c>p.o&&x.o>=p.c&&x.c<=p.o,candleTime:x.t};}
  function liquidity(c){if(c.length<7)return{status:'WAIT',side:'NONE'};const x=c[c.length-1],p=c.slice(-7,-1),hi=Math.max(...p.map(z=>z.h)),lo=Math.min(...p.map(z=>z.l)),sellSide=x.l<lo&&x.c>lo,buySide=x.h>hi&&x.c<hi;return{status:sellSide||buySide?'PASS':'WAIT',side:sellSide?'SELL_SIDE_SWEPT':buySide?'BUY_SIDE_SWEPT':'NONE',referenceHigh:hi,referenceLow:lo};}
  function structure(c){if(c.length<17)return{mss:'WAIT',bos:'WAIT'};const x=c[c.length-1],a=c.slice(-9,-1),b=c.slice(-17,-9),hi=Math.max(...a.map(z=>z.h)),lo=Math.min(...a.map(z=>z.l)),hi2=Math.max(...b.map(z=>z.h)),lo2=Math.min(...b.map(z=>z.l));return{mss:x.c>hi2?'BULLISH':x.c<lo2?'BEARISH':'WAIT',bos:x.c>hi?'BULLISH':x.c<lo?'BEARISH':'WAIT',rangeHigh:hi,rangeLow:lo};}
  function fvg(c){const out=[];for(let i=2;i<c.length;i++){const a=c[i-2],x=c[i];if(a.h<x.l)out.push({type:'BULLISH',low:a.h,high:x.l,index:i,time:x.t});if(a.l>x.h)out.push({type:'BEARISH',low:x.h,high:a.l,index:i,time:x.t});}return out.reverse().map(z=>{const later=c.slice(z.index+1),filled=z.type==='BULLISH'?later.some(x=>x.l<=z.low):later.some(x=>x.h>=z.high);return{...z,filled};}).filter(z=>!z.filled).slice(0,10);}
  function orderBlocks(c){if(c.length<8)return[];const st=structure(c),a=c.slice(-10,-1),out=[],want=st.bos==='BULLISH'||st.mss==='BULLISH'?'BULLISH':st.bos==='BEARISH'||st.mss==='BEARISH'?'BEARISH':'NEUTRAL';if(want==='NEUTRAL')return[];for(let i=a.length-1;i>=0;i--){const z=a[i];if(want==='BULLISH'&&z.c<z.o){out.push({type:'BULLISH',low:z.l,high:z.o,time:z.t});break;}if(want==='BEARISH'&&z.c>z.o){out.push({type:'BEARISH',low:z.o,high:z.h,time:z.t});break;}}return out;}
  function row(c,tf,price){if(c.length<MIN)return{tf,ready:false,bars:c.length,source:null};const last=c[c.length-1],cs=candle(c),liq=liquidity(c),st=structure(c),gaps=fvg(c),obs=orderBlocks(c),a=atr(c);let bull=50,bear=50;if(last.c>last.o)bull+=12;else if(last.c<last.o)bear+=12;if(st.mss==='BULLISH')bull+=8;if(st.mss==='BEARISH')bear+=8;if(st.bos==='BULLISH')bull+=10;if(st.bos==='BEARISH')bear+=10;if(liq.side==='SELL_SIDE_SWEPT')bull+=8;if(liq.side==='BUY_SIDE_SWEPT')bear+=8;if(cs.hammer||cs.bullishEngulfing)bull+=5;if(cs.shootingStar||cs.bearishEngulfing)bear+=5;const total=Math.max(1,bull+bear),buy=clamp(bull/total*100),sell=100-buy;return{tf,ready:true,bars:c.length,open:last.o,high:last.h,low:last.l,close:last.c,currentPrice:n(price),buyPct:Math.round(buy),sellPct:Math.round(sell),direction:buy>sell?'BULLISH':sell>buy?'BEARISH':'NEUTRAL',score:Math.round(Math.max(buy,sell)),atr:a,candle:cs,liquidity:liq,structure:st,fvg:gaps,orderBlocks:obs,orderBlock:obs[0]||null};}
  function pickZone(r,side,price){if(!r||!r.ready||price==null)return null;const want=side==='BUY'?'BULLISH':'BEARISH',candidates=[];for(const z of(r.fvg||[]))if(z.type===want)candidates.push({...z,source:'FVG'});for(const z of(r.orderBlocks||[]))if(z.type===want)candidates.push({...z,source:'ORDER_BLOCK'});const valid=candidates.filter(z=>{if(!Number.isFinite(z.low)||!Number.isFinite(z.high))return false;const low=Math.min(z.low,z.high),high=Math.max(z.low,z.high);return side==='BUY'?low<=price:high>=price;});if(!valid.length)return null;valid.sort((a,b)=>{const da=price<a.low?a.low-price:price>a.high?price-a.high:0,db=price<b.low?b.low-price:price>b.high?price-b.high:0;return da-db;});const z=valid[0];return{low:Math.min(z.low,z.high),high:Math.max(z.low,z.high),source:z.source,type:z.type,inZone:price>=Math.min(z.low,z.high)&&price<=Math.max(z.low,z.high)};}
  function analyze(){const liveQuote=live(),price=liveQuote?.price??null,rows={};let wb=0,wt=0;for(const tf of TFS){const b=feed(tf);rows[tf]=row(b.bars,tf,price);rows[tf].source=b.source;if(rows[tf].ready){wb+=rows[tf].buyPct*W[tf];wt+=100*W[tf];}}const ready=CORE.filter(tf=>rows[tf].ready).length,buy=wt?wb/wt*100:50,sell=100-buy,bias=buy>sell?'BULLISH':sell>buy?'BEARISH':'NEUTRAL',m=rows.M15,h=rows.H1||rows.H4,ref={high:h?.structure?.rangeHigh??h?.high,low:h?.structure?.rangeLow??h?.low},buyZ=pickZone(m,'BUY',price),sellZ=pickZone(m,'SELL',price),spread=n(liveQuote?.spread),maxSpread=Math.max(.01,n(process.env.VTRADE_PREMARKET_MAX_SPREAD)??.80),spreadOk=spread!=null&&spread>=0&&spread<=maxSpread,mid=Number.isFinite(ref.high)&&Number.isFinite(ref.low)?(ref.high+ref.low)/2:null,premiumDiscount=price!=null&&mid!=null?(price>mid?'PREMIUM':price<mid?'DISCOUNT':'EQUILIBRIUM'):null,pdOk=bias==='BULLISH'?premiumDiscount==='DISCOUNT':bias==='BEARISH'?premiumDiscount==='PREMIUM':false,liq=m?.liquidity?.status==='PASS',mss=m?.structure?.mss===bias,bos=m?.structure?.bos===bias,atrM=m?.atr,body=Math.abs((m?.close??0)-(m?.open??0)),displacement=atrM!=null&&body/Math.max(atrM,1e-9)>=1,fvg=!!(m?.fvg||[]).some(z=>z.type===(bias==='BULLISH'?'BULLISH':'BEARISH')),ob=!!(m?.orderBlocks||[]).some(z=>z.type===(bias==='BULLISH'?'BULLISH':'BEARISH')),momentum=Math.abs(buy-sell)>=8,execZone=bias==='BULLISH'?buyZ:bias==='BEARISH'?sellZ:null,all=ready===4&&bias!=='NEUTRAL'&&liq&&mss&&bos&&displacement&&fvg&&ob&&pdOk&&!!execZone&&momentum&&spreadOk;
    const gates={liquiditySweep:liq,mss,bos,displacement,fvg,orderBlock:ob,premiumDiscountOk:pdOk,executionZone:!!execZone,technicalMomentumOk:momentum,spreadOk,allGatesPassed:all};
    const mandatory=['liquiditySweep','mss','bos','displacement','fvg','orderBlock','premiumDiscountOk','executionZone','technicalMomentumOk','spreadOk'];
    const entryStatus=all?'ENTRY_READY':'WAIT';
    const entryReason=all?'All mandatory ICT + MTF + spread gates passed':ready<4?'Waiting for M5/M15/H1/H4 history':bias==='NEUTRAL'?'No directional MTF bias':!liq?'Waiting for liquidity sweep':!mss?'Waiting for MSS aligned with MTF bias':!bos?'Waiting for BOS aligned with MTF bias':!displacement?'Waiting for displacement':!fvg?'Waiting for aligned FVG':!ob?'Waiting for aligned Order Block':!pdOk?'Waiting for correct premium/discount location':!execZone?'No directionally valid execution zone near/at current price':!momentum?'Waiting for momentum confirmation':!spreadOk?'Waiting for acceptable broker spread':'Waiting for mandatory gates';
    const processing={stage:ready<4?'MT5_DATA_WAITING':!all?'ICT_GATES_PROCESSING':'MTF_ICT_READY',source:'MT5_AUTHORITATIVE_V4',orderAuthorization:false,aiRole:'CONFIRMATION_ONLY',telegramIndependent:true,sequence:['MT5_LIVE_QUOTE','CLOSED_CANDLES','M5','M15','H1','H4','D1','MTF_WEIGHT','CANDLE_WICK_PATTERN','LIQUIDITY_SWEEP','MSS','BOS','FVG','ORDER_BLOCK','DISPLACEMENT','PREMIUM_DISCOUNT','MOMENTUM','SPREAD','EXECUTION_ZONE','AUTHORIZATION','AI_CONFIRMATION','TELEGRAM'],completed:{mt5Quote:price!=null,closedCandles:CORE.every(tf=>rows[tf].ready),mtf:ready===4,candleWickPattern:!!m?.candle?.ready,liquiditySweep:liq,mss,bos,fvg,orderBlock:ob,displacement,premiumDiscount:pdOk,momentum,spread:spreadOk,executionZone:!!execZone,authorization:all,aiConfirmation:false,telegram:false},gatesPassed:mandatory.filter(k=>gates[k]).length,gatesRequired:mandatory.length};
    return{success:true,symbol:'XAUUSD',source:'MT5 brokerFeed',price,livePrice:price,bid:n(liveQuote?.bid),ask:n(liveQuote?.ask),spread,available:ready,required:4,complete:ready===4,optionalD1:!!rows.D1.ready,missingTimeframes:CORE.filter(tf=>!rows[tf].ready),timeframes:rows,frames:rows,buyStrengthPct:Math.round(buy),sellStrengthPct:Math.round(sell),bias,directionScore:Math.round(Math.max(buy,sell)),preAiConfidence:Math.round(50+Math.min(45,Math.abs(buy-sell)/2)),confidence:Math.round(50+Math.min(45,Math.abs(buy-sell)/2)),gates,confirmations:gates,ict:{liquiditySweep:m?.liquidity,mss:m?.structure?.mss,bos:m?.structure?.bos,fvg:m?.fvg,orderBlock:m?.orderBlock,displacement:{confirmed:displacement,atr:atrM},candle:m?.candle},zone:{buyZone:buyZ?[buyZ.low,buyZ.high]:null,sellZone:sellZ?[sellZ.low,sellZ.high]:null,buySource:buyZ?.source??null,sellSource:sellZ?.source??null,premiumDiscount,executionZoneOk:!!execZone,authorization:false},zones:{buyZone:buyZ?[buyZ.low,buyZ.high]:null,sellZone:sellZ?[sellZ.low,sellZ.high]:null},execution:{status:entryStatus,reason:entryReason,side:bias==='BULLISH'?'BUY':bias==='BEARISH'?'SELL':null,zone:execZone?[execZone.low,execZone.high]:null,source:execZone?.source??null,inZone:!!execZone?.inZone,spread,spreadOk,authorization:false},processing,workflow:{stage:ready===4?'PRE_MARKET_MTF_READY':'PRE_MARKET_MTF_WAITING',source:'MT5_AUTHORITATIVE_V4',entryAuthorization:false,orderAuthorization:false,aiRole:'CONFIRMATION_ONLY',telegramIndependent:true,executionBlocked:!all},generatedAt:new Date().toISOString()};}
  function handler(req,res){res.set('Cache-Control','no-store');res.set('X-V-TRADE-Processing','MT5_AUTHORITATIVE_V4');try{return res.json(analyze());}catch(e){console.error('[PRE-MARKET V4] ERROR',e?.stack||e);return res.status(502).json({success:false,error:String(e?.message||e),processing:{source:'MT5_AUTHORITATIVE_V4',orderAuthorization:false,aiRole:'CONFIRMATION_ONLY'}});}}
  app.options('/api/pre-market/mt5-authoritative',handler);app.get('/api/pre-market/mt5-authoritative',handler);app.options('/api/pre-market/xauusd',handler);app.get('/api/pre-market/xauusd',handler);app.options('/api/pre-market/intelligence',handler);app.get('/api/pre-market/intelligence',handler);
  console.log('[V-TRADE PRE-MARKET AUTH V4] SINGLE authoritative processing ACTIVE | M5>M15>H1>H4>D1 | ICT gates + processing trace');
})(app);
`;
  return source.replace(anchor,anchor+'\n'+code);
}
if(fs.existsSync(SERVER)){let source=fs.readFileSync(SERVER,'utf8');for(const marker of ['VTRADE_PREMARKET_AUTHORITY_ROUTE_V3','VTRADE_PREMARKET_AUTHORITY_ROUTE_V2','VTRADE_PREMARKET_AUTHORITY_ROUTE_V1']){let start=source.indexOf('/* '+marker+' */');while(start>=0){const end=source.indexOf('})(app);',start);if(end<0)break;source=source.slice(0,start)+source.slice(end+'})(app);'.length);start=source.indexOf('/* '+marker+' */');}}source=inject(source);fs.writeFileSync(SERVER,source,'utf8');}
module.exports={inject};