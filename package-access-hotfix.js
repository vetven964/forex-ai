// V-TRADE AI — package/RBAC access hotfix
const fs=require('fs'),path=require('path'),crypto=require('crypto');
const SERVER_FILE=path.resolve(__dirname,'server.js'),FRONTEND_FILE=path.resolve(__dirname,'premium-dashboard-live.html');
const originalReadFileSync=fs.readFileSync.bind(fs),MARKER='VTRADE_PACKAGE_RBAC_V2';
function patchServer(source){
 if(!source||source.includes(MARKER))return source;
 let out=source;
 const helpers=`
/* ${MARKER} */
const VTRADE_PACKAGE_FEATURES=Object.freeze({
 'FREE 7-DAY TRIAL':['dashboard','profile','settings'],
 'TRIAL':['dashboard','profile','settings'],
 'BASIC':['dashboard','terminal','signals','risk','history','profile','settings'],
 'STANDARD':['dashboard','terminal','signals','ai','news','telegram','risk','history','profile','settings'],
 'VIP PRO':['dashboard','terminal','signals','ai','news','telegram','risk','history','profile','settings'],
 'PREMIUM':['dashboard','terminal','signals','ai','news','telegram','risk','history','profile','settings']
});
function vtradePlanKey(p){return String(p||'TRIAL').trim().toUpperCase().replace(/\\s+/g,' ')}
function vtradeFeaturesForUser(u){if(String(u?.role||'').toLowerCase()==='admin')return ['*'];const f=Array.isArray(u?.features)?u.features.map(x=>String(x).trim()).filter(Boolean):[];return f.length?f:(VTRADE_PACKAGE_FEATURES[vtradePlanKey(u?.plan)]||VTRADE_PACKAGE_FEATURES.TRIAL)}
function vtradeHasFeature(u,f){const a=vtradeFeaturesForUser(u);return a.includes('*')||a.some(x=>String(x).toLowerCase()===String(f).toLowerCase())}
function requireFeature(feature){return(req,res,next)=>{const u=req.vtradeUser||getAuthSession(req);if(!u)return res.status(401).json({success:false,error:'Authentication required',code:'AUTH_REQUIRED'});req.vtradeUser=u;if(String(u.role||'').toLowerCase()==='admin'||vtradeHasFeature(u,feature))return next();return res.status(403).json({success:false,error:'Package access required',code:'PACKAGE_REQUIRED',feature,plan:u.plan||'Trial',requiredPackage:true})}}
`;
 const mark='function normalizeOrigin(value) {';if(out.includes(mark))out=out.replace(mark,helpers+'\n'+mark);
 const routeMark="app.get('/api/analysis/xauusd',async(req,res)=>{";
 const routes=`
app.get('/api/auth/access',requireAuth,(req,res)=>{const u=req.vtradeUser;res.set('Cache-Control','no-store');res.json({success:true,user:{id:u.id,name:u.name,email:u.email,role:u.role,plan:u.plan||'Trial'},isAdmin:String(u.role||'').toLowerCase()==='admin',features:vtradeFeaturesForUser(u),allFeatures:['dashboard','terminal','signals','ai','news','telegram','risk','history','profile','settings']})});
app.get('/api/admin/sessions',requireAuth,requireRole('admin'),(req,res)=>{const now=Date.now(),sessions=[];for(const[token,s]of authSessions.entries()){if(!s||!s.expiresAt||now>=s.expiresAt){authSessions.delete(token);continue}sessions.push({id:s.id,name:s.name,email:s.email,role:s.role,plan:s.plan||'Trial',createdAt:s.createdAt,lastSeenAt:s.lastSeenAt,expiresAt:s.expiresAt,active:true,tokenFingerprint:crypto.createHash('sha256').update(token).digest('hex').slice(0,10)})}sessions.sort((a,b)=>Number(b.lastSeenAt||0)-Number(a.lastSeenAt||0));res.set('Cache-Control','no-store');res.json({success:true,count:sessions.length,sessions})});
`;
 if(out.includes(routeMark))out=out.replace(routeMark,routes+'\n'+routeMark);
 out=out.replace("app.get('/api/analysis/xauusd',async(req,res)=>{","app.get('/api/analysis/xauusd',requireFeature('terminal'),async(req,res)=>{");
 out=out.replace("app.get('/api/ai/analysis/xauusd',async(req,res)=>{","app.get('/api/ai/analysis/xauusd',requireFeature('ai'),async(req,res)=>{");
 out=out.replace("app.get('/api/news/xauusd', async (_req,res)=>{","app.get('/api/news/xauusd',requireFeature('news'),async (_req,res)=>{");
 out=out.replace("app.get('/api/telegram/status',async(req,res)=>{","app.get('/api/telegram/status',requireFeature('telegram'),async(req,res)=>{");
 out=out.replace("app.post('/api/telegram/connect',telegramMutationLimit,async(req,res)=>{","app.post('/api/telegram/connect',requireFeature('telegram'),telegramMutationLimit,async(req,res)=>{");
 out=out.replace("app.post('/api/telegram/test',telegramMutationLimit,async(req,res)=>{","app.post('/api/telegram/test',requireFeature('telegram'),telegramMutationLimit,async(req,res)=>{");
 out=out.replace("app.post('/api/telegram/disconnect',telegramMutationLimit,(req,res)=>{","app.post('/api/telegram/disconnect',requireFeature('telegram'),telegramMutationLimit,(req,res)=>{");
 console.log('[V-TRADE PACKAGE] ADMIN=ALL | USER=PURCHASED FEATURES ONLY | ACTIVE SESSIONS=ADMIN ONLY');return out;
}
function patchFrontend(){try{if(!fs.existsSync(FRONTEND_FILE))return;let html=fs.readFileSync(FRONTEND_FILE,'utf8'),m='VTRADE_PACKAGE_ACCESS_UI_V2';if(html.includes(m))return;
 const css=`<style id="${m}">.vtrade-locked{opacity:.42!important;filter:grayscale(.55);pointer-events:none!important;position:relative}.vtrade-locked::after{content:'🔒 PACKAGE';font-size:8px;position:absolute;right:8px;top:50%;transform:translateY(-50%);color:#f2c94c}.vtrade-package-banner{margin:10px 0;padding:11px 13px;border:1px solid #6940c9;border-radius:12px;background:#17102e;color:#d8c8ff;font-size:11px}.vtrade-session-table{width:100%;border-collapse:collapse;min-width:760px}.vtrade-session-table th,.vtrade-session-table td{padding:9px;border-bottom:1px solid #17263b;text-align:left;font-size:10px}.vtrade-session-table th{color:#8493ab;background:#080f1b}</style>`;
 const js=`<script id="${m}">(function(){const API=String(localStorage.getItem('vtrade_api')||'https://forexai-6xw6.onrender.com').replace(/\\/$/,''),token=()=>sessionStorage.getItem('vtrade_auth_token')||sessionStorage.getItem('vtrade_auth')||'';const features=['dashboard','terminal','signals','ai','news','telegram','risk','history','profile','settings'];function apply(d){const fs=new Set((d.features||[]).map(x=>String(x).toLowerCase())),admin=d.isAdmin===true;document.body.dataset.vtradePlan=d.user?.plan||'Trial';document.body.dataset.vtradeRole=d.user?.role||'user';document.querySelectorAll('.nav button,[data-target]').forEach(el=>{const f=String(el.dataset.target||'').toLowerCase()||features.find(x=>String(el.textContent||'').toLowerCase().replace(/[^a-z]+/g,' ').includes(x));if(!f)return;const ok=admin||fs.has(f);el.classList.toggle('vtrade-locked',!ok);if(!ok){el.title='🔒 Package required — '+(d.user?.plan||'Trial');el.dataset.packageLocked='true'}});const host=document.querySelector('.wrap')||document.querySelector('.content')||document.querySelector('main');if(host&&!document.getElementById('vtradePackageBanner')){const b=document.createElement('div');b.id='vtradePackageBanner';b.className='vtrade-package-banner';b.textContent='Package: '+(d.user?.plan||'Trial')+' · Only purchased features are enabled.'+(admin?' · ADMIN: all access + all sessions':'');host.prepend(b)}window.VTRADE_ACCESS=d;if(admin)loadSessions()}async function loadSessions(){const panel=document.getElementById('accountAdminPanel');if(!panel)return;let box=document.getElementById('vtradeAllSessions');if(!box){box=document.createElement('div');box.id='vtradeAllSessions';box.className='admin-table-wrap';box.style.marginTop='12px';box.innerHTML='<div style="padding:11px;border-bottom:1px solid #1d2c44"><b>All Active Sessions</b><span style="float:right;color:#8493ab;font-size:10px">ADMIN ONLY</span></div><div style="overflow:auto"><table class="vtrade-session-table"><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Package</th><th>Login</th><th>Last Active</th><th>Status</th></tr></thead><tbody id="vtradeSessionRows"><tr><td colspan="7">Loading…</td></tr></tbody></table></div>';panel.querySelector('.card')?.appendChild(box)}try{const r=await fetch(API+'/api/admin/sessions',{headers:{'x-vtrade-auth':token()},credentials:'include',cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Sessions unavailable');const rows=document.getElementById('vtradeSessionRows'),list=Array.isArray(d.sessions)?d.sessions:[];rows.innerHTML=list.map(s=>'<tr><td>'+String(s.name||'').replace(/[<>]/g,'')+'</td><td>'+String(s.email||'').replace(/[<>]/g,'')+'</td><td>'+String(s.role||'').replace(/[<>]/g,'')+'</td><td>'+String(s.plan||'').replace(/[<>]/g,'')+'</td><td>'+new Date(s.createdAt).toLocaleString()+'</td><td>'+new Date(s.lastSeenAt).toLocaleString()+'</td><td><b style="color:#22e58a">ACTIVE</b></td></tr>').join('')||'<tr><td colspan="7">No active sessions.</td></tr>'}catch(e){const rows=document.getElementById('vtradeSessionRows');if(rows)rows.innerHTML='<tr><td colspan="7">'+String(e.message||'Sessions unavailable').replace(/[<>]/g,'')+'</td></tr>'}}async function load(){const t=token();if(!t)return;try{const r=await fetch(API+'/api/auth/access',{headers:{'x-vtrade-auth':t},credentials:'include',cache:'no-store'}),d=await r.json().catch(()=>({}));if(r.ok)apply(d)}catch(_){}}load()})();</script>`;html=html.replace('</head>',css+'\n</head>').replace('</body>',js+'\n</body>');fs.writeFileSync(FRONTEND_FILE,html,'utf8');console.log('[V-TRADE PACKAGE] package access UI applied')}catch(e){console.warn('[V-TRADE PACKAGE] frontend patch skipped:',e.message)}}
const previousReadFileSync=fs.readFileSync.bind(fs);fs.readFileSync=function(file,...args){const source=previousReadFileSync(file,...args);return path.resolve(String(file))===SERVER_FILE&&typeof source==='string'?patchServer(source):source};patchFrontend();
